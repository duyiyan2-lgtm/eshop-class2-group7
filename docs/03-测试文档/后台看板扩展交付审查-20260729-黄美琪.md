# 后台看板扩展交付审查（黄美琪）

> 日期：2026-07-29
> 审查人：组长（祝意远）
> 交付人：黄美琪（组员 3）
> 交付包：`组员3_黄美琪_2026-07-29_交付`
> 建议决策：**不整包合并；禁止合入 Mock；主仓库继续使用现有看板扩展实现**

## 一、交付包结构

| 项 | 结果 |
|----|------|
| 文件数 | **14**（精简，明显好于整仓副本） |
| 范围 | 以后台看板为主，另夹带部分 API/布局文件 |
| `node_modules` / `target` / `dist` / `.env` | 未发现 |
| 是否整仓覆盖风险 | 低（文件少），但含 **Mock 与旧 API 包装** 风险高 |

### 文件清单

```text
backend/.../admin/dashboard/
  DashboardController.java
  DashboardMapper.java
  DashboardService.java
  DashboardSummaryResponse.java
  SalesTrendResponse.java
  TopProductResponse.java
frontend/src/views/admin/AdminHomeView.vue
frontend/src/api/admin.js          ⚠ 含 mock_api
frontend/src/api/auth.js           ⚠ 自动 Mock 登录
frontend/src/api/address.js        ⚠ Mock
frontend/src/api/cart.js           ⚠ Mock
frontend/src/layouts/MobileShopLayout.vue
frontend/src/utils/cartBadge.js    （与主仓相同）
frontend/src/mock/data.js          ⚠ 明确 Mock 数据
```

## 二、安全检查（关键）

| 检查项 | 结果 | 处理 |
|--------|------|------|
| `mock_api` / `mock_token_` | **有** | **拒绝合并** |
| 后端失败自动 Mock 登录（`auth.js`） | **有** | **拒绝合并** |
| 看板业务后端新增接口 | 有 | 可仅作对照，不整文件覆盖 |
| 是否改 Flyway / router | 未见 | 符合边界 |

**一票否决点：** 交付前端重新引入了此前组长明确禁止的 **自动 Mock 降级**。一旦合入 `auth.js` / `admin.js` / `cart.js` / `address.js` / `mock/data.js`，会掩盖后端故障并可用固定演示数据绕过真实接口。

## 三、业务实现审查

### 3.1 后端（方向正确，细节与冻结/主仓有差异）

| 接口 | 交付 | 冻结 v1.2 / 主仓库 | 评价 |
|------|------|-------------------|------|
| `GET /admin/dashboard/summary` | 保留 | 保留 | ✅ |
| `GET /admin/dashboard/sales-trend` | 返回 **数组** `[{date,totalAmount,orderCount}]` | 返回 **对象** `{days, points:[{date,orderCount,salesAmount}]}` | ⚠ 字段名/结构不同 |
| `GET /admin/dashboard/top-products` | 返回 **数组** `[{productId,productName,totalQuantity,totalAmount}]` | 返回 **对象** `{limit, items:[{...,soldQuantity,salesAmount}]}` | ⚠ 字段名/结构不同 |
| days/limit 范围校验 | 基本无 | 1–30 / 1–20 | ⚠ 交付较弱 |
| 金额口径 | PAID/SHIPPED/COMPLETED | 相同 | ✅ |
| Top 是否只计在售商品 | **未过滤 ON_SALE** | 主仓过滤在售 | ⚠ |
| 无单日补 0 | 有 | 有 | ✅ |
| 权限 | `@PreAuthorize ADMIN` | 相同 | ✅ |

### 3.2 前端 AdminHomeView

- 已接入趋势条/表与 Top 列表，旧 summary 卡片与热销区思路在。
- 字段绑定交付后端结构（`totalAmount` / `totalQuantity`），**不能**直接对接主仓当前 API 字段而不改前端。
- 若强行合入交付 `admin.js`，会同时带入 Mock 分支。

### 3.3 主仓库现状（已可演示）

主仓库已有：

- `sales-trend` / `top-products`（结构见冻结文档）
- `AdminHomeView` 趋势与 Top 展示
- `DashboardIntegrationTest` 含权限与参数校验

**功能上主仓已覆盖组员任务目标。**

## 四、联调/对比测试结论

### 交付包静态测试

| 场景 | 结论 |
|------|------|
| 包是否可直接整包覆盖主仓 | **否**（Mock + 响应结构冲突 + 多余 API 文件） |
| 后端逻辑是否可独立成立 | **基本可以**（在不校验参数、不筛在售的前提下） |
| 前端是否允许合入 | **否**（Mock）；`AdminHomeView` 需按主仓字段改才能用 |

### 主仓库实机 API（审查时环境）

对当前已启动的主仓库做了实机抽检（管理员登录后）：

- `GET /admin/dashboard/summary` 可用
- `GET /admin/dashboard/sales-trend?days=7` 可用
- `GET /admin/dashboard/top-products?limit=5` 可用
- 普通用户访问扩展接口应 403（与既有权限策略一致）

## 五、处理决策（建议已定）

1. **不整包合并**交付目录。
2. **禁止合并**：`mock/data.js`、`auth.js`、`address.js`、`cart.js` 及带 `mock_api` 的 `admin.js`。
3. **主仓库继续使用现有看板扩展实现**（与浏览历史策略一致）。
4. 过程上可认定：黄美琪完成了看板扩展的主要开发尝试；技术合入以主仓为准。
5. 反馈组员：去掉 Mock 后，仅交 `dashboard` 后端 + 不依赖 Mock 的 `AdminHomeView`/`admin.js` 增量亦可再审；但当前主仓已可演示，不必强求再交整包。

## 六、可转发反馈（给黄美琪）

> 看板交付已收到，结构精简、后端趋势/Top 思路正确，这点很好。
> 审查结论：
> 1）`auth/cart/address/admin` 含 `mock_api` 自动 Mock，**不能合入**（与全组约定冲突）。
> 2）`sales-trend`/`top-products` 响应结构与冻结文档不一致（主仓为 `{days,points}` / `{limit,items}` 及统一字段名）。
> 3）days/limit 边界与 Top 在售过滤建议补强。
> 处理：不整包合并；主仓库继续用现有看板实现与演示。你的交付会记入审查材料。请补今日日报。
