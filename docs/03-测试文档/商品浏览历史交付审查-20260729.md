# 商品浏览历史交付审查

> 日期：2026-07-29
> 审查人：组长（祝意远）
> 交付人：吴松青（2444310213）
> 交付包：`eshop-class2-group7-browse-history-source`
> 决策：**不整包合并；主仓库继续使用现有浏览历史实现**

## 一、审查结论

| 项 | 结论 |
|----|------|
| 是否整包覆盖主仓库 | **否** |
| 主仓库浏览历史是否保留 | **是**（当前已接入路由、可演示） |
| 组员是否完成主要工作 | **是**（四接口 + PC/手机列表 + 详情静默记录 + 日报） |
| 是否记入过程材料 | **是**（日报已入库，本审查记录归档） |

## 二、交付包安全检查

| 检查项 | 结果 |
|--------|------|
| 包形态 | 完整项目副本（约 295 文件），非精简业务 diff |
| `node_modules` / `target` / `dist` / `.env` | 未发现 |
| 自动 Mock 登录 | 未发现 |
| 是否擅自修改 `router/index.js` 并要求整仓覆盖 | 未接入公共 router（符合约定） |
| 是否自建 Flyway 版本 | 使用既有 V6 表，未另开迁移编号 |

**处理原则：** 只认业务审查结论与过程材料，**禁止**整目录覆盖 `eshop-release-v1.0`。

## 三、组员交付有效范围

### 后端

- 包：`com.eshop.backend.browse`
- 接口：
  - `POST /browse-history/{productId}`
  - `GET /browse-history`
  - `DELETE /browse-history/{productId}`
  - `DELETE /browse-history`
- 测试：`BrowseHistoryIntegrationTest`（交付包内有）

### 前端

- `api/browseHistory.js`
- `PcHistoryView.vue` / `MobileHistoryView.vue`
- 商品详情静默记录（交付包内）
- 建议路径：`/pc/history`、`/m/history`（未改公共 router）

### 过程

- 日报：`reports/daily/吴松青_2444310213/20260729-吴松青.md`（已拷贝入主仓库）

## 四、与主仓库实现对比（为何不整包合并）

| 点 | 组员交付 | 主仓库现网/现分支 | 取舍 |
|----|----------|-------------------|------|
| 接口路径 | `/browse-history` | 相同 | 对齐 |
| 仅在售可记录 | 未强制 ON_SALE | 强制 ON_SALE | **保留主仓库** |
| 列表字段 | 名称/图片 | 名称/图/价/库存 | **保留主仓库** |
| 列表过滤下架 | 弱 | 只展示在售 | **保留主仓库** |
| 路由与导航 | 未接入 | 已接入并可演示 | **保留主仓库** |
| 晒图/看板等扩展 | 整包含旧副本风险 | 已含后续修复 | **禁止整包覆盖** |

主仓库浏览历史关键位置（以当前仓库为准）：

- 后端：`eshop-release-v1.0/backend/.../browse/`
- 前端：`PcBrowseHistoryView.vue`、`MobileBrowseHistoryView.vue`
- 路由：`/pc/history`、`/m/history`
- API：`frontend/src/api/browseHistory.js`

## 五、组长操作记录

1. 审查交付包结构与浏览历史相关文件。
2. **不合并**业务源码到主仓库（主仓库已具备等价且更完整的实现）。
3. 拷贝组员 2026-07-29 日报至主仓库 `reports/daily/`。
4. 写入本审查记录，作为验收/答辩过程材料。

## 六、对组员反馈要点（已决策）

1. 交付方向正确：四接口、列表、静默记录、未擅改 router/Flyway。
2. 整仓副本不会整包合入；主仓库以集成分支现有浏览历史为准。
3. 日报已纳入过程材料；模块讲解可按「浏览历史全链路」准备答辩。
4. 有余力可按「仅在售可记录 + 列表展示价库存」自行对照主仓库学习差异，无需再交整包。

## 七、后续

- 演示使用主仓库已启动环境：`http://localhost:8088`（PC 导航「浏览历史」/ 手机个人中心入口）。
- 其他组员交付（晒图、看板等）同样执行「只提取业务、禁止整包覆盖」策略。
