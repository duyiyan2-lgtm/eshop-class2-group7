# 缺陷修复记录（2026-07-29）

## 修复项

| 编号 | 级别 | 问题 | 处理 |
|------|------|------|------|
| BUG-001 | P0 | `uploadUserImage` 手动设置 `Content-Type: multipart/form-data` 导致 boundary 缺失，评价晒图上传失败 | 去掉手动头，与管理员上传一致 |
| BUG-002 | P1 | PC 订单详情重复评价时先关闭弹窗仍弹出错误提示 | 重复评价走 `info` 并 return，不再 `error` |
| BUG-003 | P2 | 手机浏览历史 van-list + onMounted 双加载，下拉刷新可能卡住 | 改为与收藏页一致的分页 + pull-refresh |

## 完善项

1. PC/手机评价评分校验改为兼容非整数评分展示值。
2. 后台评价管理展示晒图缩略图，可点击打开原图。
3. 商品公开评价 / 我的评价晒图可点击打开。
4. 新增 `BrowseHistoryIntegrationTest`；看板补充 sales-trend / top-products 权限与参数测试；评价响应断言 `imageUrls`。

## 验证

- 后端：`mvn test`
- 前端：`npm run build`
