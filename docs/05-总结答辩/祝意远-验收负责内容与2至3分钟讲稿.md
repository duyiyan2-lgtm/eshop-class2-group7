# 祝意远：验收负责内容与 2～3 分钟讲稿

> 姓名：祝意远　学号：2444310229
>
> 项目角色：组长 / 架构 / 后端与集成负责人
>
> 配套 PPT：`E-Shop项目验收汇报-祝意远-2至3分钟.pptx`

## 一、我负责的部分

### 1. 架构与公共基建

- 确定 PC 消费者端、手机 H5、Web 管理端的三端方案，并建立统一入口。
- 统一 Vue 3 路由、请求封装、JWT 登录和买家/商家/管理员角色权限。
- 冻结核心接口、数据库结构和公共配置，控制多人开发中的重复实现与合并冲突。

### 2. 后端关键业务

- 用户认证、BCrypt 密码、JWT 鉴权以及 401/403 权限边界。
- 商品 SKU、购物车、地址、订单、模拟支付、发货、收货和评价主链路的集成与兜底。
- 服务端计价、订单快照、库存条件扣减、订单状态机和取消库存回补。
- C03 订单超时自动取消及支付并发冲突处理。
- 商家数据隔离：商品、库存、订单、评价、看板和日志按 `seller_id` 隔离，数据库迁移到 Flyway V9。

### 3. 集成、质量与部署

- 审查组员交付，处理路由、接口、迁移和公共文件冲突，拒绝 Mock 数据进入最终版本。
- 组织后端自动化、前端生产构建、真实 MySQL API 冒烟和跨角色验收。
- 使用 Docker Compose 部署 MySQL、Spring Boot 和 Nginx 前端，完成域名、HTTPS、CORS 和健康检查。
- 汇总日报、周报、设计文档、测试报告、演示脚本和验收材料。

## 二、2～3 分钟逐页讲稿

### 第 1 页：项目介绍（约 15 秒）

大家好，我是第 7 组组长祝意远。我们完成的是一个三端、多角色的 E-Shop 电子商城。我的主要工作是架构、后端关键业务、多人代码集成、测试与服务器部署。

### 第 2 页：我的职责（约 30 秒）

我负责的不是单一页面，而是三个层面。第一，统一三端架构、接口和权限边界；第二，完成订单库存、超时取消和商家隔离等关键后端；第三，对组员代码做差异审查和合并，统一完成自动化测试、Docker 部署以及验收材料。

### 第 3 页：三端四入口（约 30 秒）

系统按课程要求交付 PC、手机 H5 和 Web 管理三端，并进一步拆成四个入口。它们共享同一套 Spring Boot API 和 MySQL 数据，JWT 负责识别买家、商家和管理员。这样既减少重复开发，也保证不同终端看到同一份商品、库存和订单状态。

### 第 4 页：核心交易链（约 40 秒）

核心亮点是一条完整、受约束的交易链。买家从 SKU 选择到下单支付，金额由服务端计算，库存用条件更新防止超卖；商家只能看到并发货自己的订单，`seller_id` 从数据库到接口形成隔离；买家收货评价后，管理员还能做账号、评价和运营监管。订单状态机同时限制重复支付、重复发货和错误状态操作。

### 第 5 页：测试与部署（约 30 秒）

为了证明项目不是只在页面上能点，我建立了三层验证。第一层是 26 项后端自动化测试全部通过；第二层是 Flyway 迁移到 V9，并在真实 MySQL 完成完整 API 冒烟；第三层是 Docker 三服务、HTTPS 正式域名和健康接口持续可用。

### 第 6 页：结论（约 15 秒）

因此，我们的项目已经达到可运行、可验证、可交付的验收状态。接下来我会用正式环境演示买家下单、商家发货、买家收货评价这一条最能体现系统完整性的链路。谢谢老师。

预计总时长约 2 分 40 秒。现场如果语速偏慢，可省略第 3 页最后一句和第 4 页最后一句，将时长控制在 2 分 30 秒左右。

## 三、老师追问时的快速回答

### 1. SKU 是什么？

SKU 是最小可售库存单位。同一商品的颜色、容量等规格可能有不同价格和库存，所以公共商品信息放商品表，规格、价格和库存放 SKU 表。

### 2. 如何避免库存变成负数？

库存扣减 SQL 同时要求 `stock >= quantity`，用受影响行数判断是否成功；任一 SKU 扣减失败，整个下单事务回滚。

### 3. 商家数据如何隔离？

商品和订单保存 `seller_id`，商家身份由后端从 JWT 获取，不能由前端传入。商品、SKU、库存、订单、评价、看板和日志查询都按当前商家过滤；管理员保留全量权限。

### 4. 为什么手机端选择 H5？

课程要求的是手机端，没有要求 APK、iOS 或小程序。Vue 3 + Vant H5 能直接在手机浏览器使用、无需安装，还能与 PC 端复用同一后端 API，适合项目周期。

### 5. 401 和 403 有什么区别？

401 表示没有登录或 Token 无效；403 表示已经登录，但当前角色没有执行该操作的权限。

### 6. 支付与超时取消同时发生怎么办？

支付和取消都要求订单当前状态必须为待支付，通过数据库条件更新竞争状态转换。只有一个请求能成功，取消方只有成功取得状态后才回补库存，因此不会同时支付和取消，也不会重复回补。

## 四、上台前 1 分钟检查

- [ ] PPT 使用全屏放映，讲稿只放在演讲者备注或手机中。
- [ ] 正式地址 `https://eshop.zhuyiyuan9.top` 可以打开。
- [ ] 管理员、商家和买家演示账号可以登录，密码不出现在 PPT 或投影中。
- [ ] 准备一笔待支付订单、一笔可发货订单和一笔可评价订单。
- [ ] 健康接口返回 `UP`，Docker 三服务正常。
- [ ] 关闭通知、聊天窗口和无关标签页。
- [ ] 不现场修改数据库、Flyway、管理员密码或核心配置。
