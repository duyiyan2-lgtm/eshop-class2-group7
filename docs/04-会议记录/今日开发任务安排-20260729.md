# E-Shop 今日开发任务安排

> 日期：2026-07-29
> 组长：祝意远
> 项目人数：4 人
> 今日目标：在不破坏三端核心交易链的前提下，冻结并启动「浏览历史 / 评价晒图 / 看板图表」三项扩展；组长完成迁移预留、公共上传与集成验收准备。

## 一、当前基线

```text
项目目录：eshop-release-v1.0/
工作分支建议：chore/day-extension（或基于 master / chore/extension-integration 拉出）
数据库：Flyway 目标版本 V7（组长已提交 V6、V7 脚本）
```

已完成：核心交易链、收藏、评价、评分统计、评价审核、热销、库存预警、订单导出、后台 summary、C03 超时取消。

## 二、今日任务分配

| 负责人 | 今日任务 | 建议分支 | 交付标准 |
|--------|----------|----------|----------|
| 祝意远（组长） | 预留 V6/V7；冻结接口文档；登录用户上传 `POST /files/upload`；错误码；C03 演示脚本；晚间集成回归 | `chore/day-extension` | 迁移可执行；文档齐全；测试不回归 |
| 组员 2 | **商品浏览历史** 全链路（表已建） | `feature/browse-history` | 记录/列表/删除/清空 + PC/手机入口 |
| 组员 3 | **后台看板** 销售趋势 + Top 商品 | `feature/dashboard-charts` | 两个只读接口 + 首页展示 |
| 组员 4 | **评价晒图**（列 `images_json` 已加） | `feature/review-images` | 上传 0～3 图 + 列表展示 |

详细接口与规则：`docs/02-设计文档/今日扩展接口冻结-v1.2.md`。

## 三、组长今日完成情况（开发事项）

### 1. 数据库迁移预留

- [x] `V6__create_product_browse_history.sql` — 浏览历史表与唯一约束
- [x] `V7__add_review_images.sql` — 评价 `images_json` 字段
- [x] `schema-test.sql` 同步 H2 集成测试结构

### 2. 公共能力与错误码

- [x] `POST /files/upload` — 登录用户图片上传（评价晒图依赖）
- [x] 管理员上传改为 `uploadAdminImage`，避免与用户上传日志混淆
- [x] 预留错误码：`40033` / `40034` / `40418`

### 3. 文档

- [x] `docs/02-设计文档/今日扩展接口冻结-v1.2.md`
- [x] `docs/02-设计文档/C03演示脚本.md`
- [x] `docs/04-会议记录/今日开发任务安排-20260729.md`（本文）
- [x] `docs/04-会议记录/扩展合入检查清单-20260729.md`

### 4. C03 演示准备

- [x] `.env.example` 补充演示超时说明
- [x] 演示脚本：超时取消 + 支付冲突原理

### 5. 组长业务兜底（已完成）

因需尽快形成可演示闭环，组长已直接完成三项扩展业务实现：

- [x] 浏览历史：后端接口 + PC/手机列表 + 详情静默记录 + 路由入口
- [x] 评价晒图：`images_json` 读写 + `/files/upload` + 订单评价上传 + 列表展示
- [x] 看板图表：`sales-trend` / `top-products` + 后台首页展示

### 6. 组员「浏览历史」交付处理（已决策）

- [x] 收到吴松青交付包 `eshop-class2-group7-browse-history-source`
- [x] **不整包合并**；主仓库继续使用现有浏览历史实现
- [x] 组员日报已入库；审查记录见 `docs/03-测试文档/商品浏览历史交付审查-20260729.md`

### 7. 待环境就绪后执行

- [ ] `mvn test` / 前端生产构建 / Docker / API 冒烟
- [ ] 三端关键路径人工点检

## 四、组员开发边界

1. 不得修改 Flyway V1～V7 脚本内容与版本号。
2. 不得直接改 `frontend/src/router/index.js`（提交建议路径，组长统一接入）。
3. 不得修改 v1 冻结接口与请求 DTO 字段集合。
4. 不得引入自动 Mock 登录或业务 Mock 数据。
5. 交付精简源码：只含业务相关 `.java` / `.vue` / `.js`，无依赖与构建产物。

## 五、建议合入顺序

```text
同步 chore/day-extension
→ 合并 feature/browse-history
→ 合并 feature/review-images
→ 合并 feature/dashboard-charts
→ 组长改 router 接入页面
→ mvn test
→ 前端 build
→ docker compose up -d --build
→ api-smoke-test.ps1
→ 人工点检
```

## 六、今日结论（组长部分）

组长侧**开发与预留工作已完成**，组员可基于冻结文档并行开发。全组「今日目标」是否完成，取决于三位组员交付与晚间集成结果。
