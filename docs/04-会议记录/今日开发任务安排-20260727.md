# E-Shop 今日开发任务安排

> 日期：2026-07-27  
> 组长：祝意远（组员 1）  
> 项目人数：4 人  
> 编号说明：组长保留姓名，其他成员沿用初版组员编号。  
> 今日目标：完成手机 H5 端“浏览商品 → 选择 SKU → 加入购物车 → 选择地址 → 确认订单 → 模拟支付”的最小可运行闭环。

## 当前剩余功能核对

本次任务分配以仓库当前代码为准，不重复安排已经完成的功能。

| 优先级 | 当前剩余内容 | 当前状态 | 负责人 |
|---|---|---|---|
| 已完成 | 手机商品列表与商品详情 | 已合并真实商品列表与详情页，完成构建、接口和实际渲染测试 | 组员 2 交付，祝意远集成 |
| 已完成 | 手机购物车与收货地址 | 已合并真实页面，完成构建、接口和登录保护测试 | 组员 3 交付，祝意远集成 |
| 已完成 | 手机确认订单 | 已合并真实确认订单页，完成构建、接口和登录保护测试 | 组员 4 交付，祝意远集成 |
| 待最终验收 | 手机完整交易链联调 | 所有 P0 页面已接入，生产构建和真实 API 链路通过，待登录后逐页人工点击验收 | 祝意远（组员 1） |
| 已完成 | 手机个人中心 | 已接入真实用户信息、订单、地址、购物车和退出入口 | 组员 3 交付，祝意远集成 |
| 已完成 | Web 后台用户管理 | 已接入搜索、筛选、分页和启用/禁用功能 | 组员 3 交付，祝意远集成 |
| 已完成 | PC 个人资料维护 | 已接入资料查询、校验和保存功能 | 组员 3 交付，祝意远集成 |
| P2 | 全端回归、兼容性、测试材料和答辩材料 | 待主链路完成后集中处理 | 祝意远（组员 1）组织，全体组员参与 |

今天不重复开发以下已完成内容：

- 已冻结并通过既有测试的后端核心接口。
- PC 商品、购物车、地址、下单、订单和模拟支付页面。
- Web 后台分类、商品、订单和操作日志页面。
- 手机订单列表、订单详情和模拟支付页面。
- 手机商品列表、搜索/分类、商品详情、SKU 选择和加入购物车入口。

### 今日进度更新

- 组员 2 已交付 `MobileProductListView.vue` 和 `MobileProductDetailView.vue`。
- 祝意远已完成代码审查，只提取两个真实新增页面，没有覆盖交付包中其余仅换行符不同的 23 个旧页面。
- 已修复分页失败后重试状态未清除、详情图片失败无降级、详情异步请求可能覆盖新路由数据三个问题。
- 已将 `/m/products` 和 `/m/products/:id` 接入真实页面。
- 前端生产构建、Docker 镜像构建、API 冒烟、商品列表接口、商品详情接口和手机尺寸实际渲染测试均通过。
- 组员 4 已交付 `MobileCheckoutView.vue`；祝意远逐文件比较完整交付目录后，只提取该真实新增页面，没有复制 `node_modules`、`.env`、IDE 配置和旧版路由。
- 已将 `/m/checkout` 接入真实页面，同时保留已经完成的商品列表、商品详情和订单路由。
- 已修复确认订单页加载失败无重试、异常状态仍显示提交栏、商品图片失败无降级和状态残留问题。
- 确认订单页生产构建、Docker 重建、未登录路由保护和真实 MySQL API 完整交易链测试均通过。
- 组员 3 已交付 13 个前端源码文件，覆盖手机购物车、地址、个人中心、PC 个人资料和 Web 后台用户管理。
- 祝意远未接受交付中的旧版路由和自动 Mock 降级逻辑，避免商品详情、确认订单回退占位页以及真实接口故障被模拟数据掩盖。
- 已按当前基线最小接入 5 个真实页面、2 个真实 API 接口、PC/后台菜单和对应路由。
- 已修复后台切换分页大小跳错页、地址表单校验失败仍关闭、购物车并发更新覆盖、刷新失败仍提示成功、图片失败无降级和个人资料未刷新等问题。
- 前端生产构建成功转换 2145 个模块；Docker 重建和真实 MySQL API 完整冒烟全部通过。
- `/m/cart`、`/m/addresses`、`/m/profile`、`/pc/profile` 和 `/admin/users` 直接路由均返回 200，未登录访问会分别进入正确的手机、PC 或后台登录页。
- 当前没有尚未接入的手机 P0 页面；只剩登录后的完整可视化交易链人工验收和 P2 收尾工作。

## 一、统一开发基线

所有组员开始开发前，必须同步组长个人 Gitee 仓库的最新 `master`：

```text
仓库：https://gitee.com/du-yiyan/eshop-class2-group7
分支：master
基线提交：7a1e44c
项目目录：eshop-release-v1.0/
```

同步后执行：

```bash
git log -1 --oneline
```

确认输出中包含：

```text
7a1e44c fix(mobile): 完善订单列表详情与支付交互
```

每位组员应从最新基线创建自己的功能分支，禁止直接在 `master` 上开发。

## 二、今日任务分配

| 组员 | 负责模块 | 今日必须完成 | 建议分支 |
|---|---|---|---|
| 祝意远（组长、组员 1） | 公共基建、集成与测试 | 接入本日新增页面路由；审查三位组员代码；完成手机全链路联调、构建和缺陷清单 | `chore/integration-release` |
| 组员 2（商品模块负责人） | 手机商品模块 | 将手机欢迎页替换为真实商品列表；完成分类/搜索、商品详情、SKU 选择和加入购物车 | `feature/mobile-catalog` |
| 组员 3（用户、地址与购物车模块负责人） | 用户、地址与购物车模块 | 已交付手机购物车、收货地址、个人中心，并补充 PC 个人资料和后台用户管理 | `feature/mobile-user-cart` |
| 组员 4（订单、支付与 C03 模块负责人） | 订单、支付与 C03 模块 | 已交付手机确认订单页面；继续配合回归订单、支付及 C03 边界场景 | `feature/mobile-order-payment` |

## 三、各组员交付标准

### 1. 祝意远（组长、组员 1）：公共基建、集成与测试

#### 今日工作

1. 维护公共文件：
   - `frontend/src/router/index.js`
   - `frontend/src/layouts/MobileShopLayout.vue`
   - `frontend/src/api/http.js`
   - `frontend/src/stores/auth.js`
2. 向三位组员确认接口、文件范围和路由映射。
3. 审查三位组员提交的页面、API、异常处理和移动端适配。
4. 将商品详情、购物车、地址和确认订单路由从 `MobilePlaceholderView` 切换到真实页面。
5. 统一接入手机端路由，不让多人同时修改 `router/index.js`。
6. 组织完整交易链联调并记录缺陷。

#### 验收标准

- 三位组员页面能够从手机底部导航或业务按钮进入。
- 未登录访问购物车、地址、确认订单和订单页面时正确跳转登录。
- 页面刷新不出现 404、白屏或重复请求。
- `router/index.js` 中只允许个人中心保留占位页；其余 P0 路由必须使用真实页面。
- 前端生产构建通过。
- Docker 三个服务正常运行。
- API 冒烟测试通过。

### 2. 组员 2：手机商品模块

#### 建议文件

```text
frontend/src/views/mobile/MobileProductListView.vue
frontend/src/views/mobile/MobileProductDetailView.vue
```

可根据实际情况复用或改造 `MobileHomeView.vue`，但 `/m/products` 最终不能继续显示欢迎内容。

#### 今日工作

1. 完成商品列表和分页加载。
2. 完成分类筛选和关键词搜索。
3. 完成商品详情。
4. 完成 SKU 规格选择、价格和库存联动。
5. 完成加入购物车操作。
6. 处理加载、空数据、无搜索结果和接口失败状态。

#### 验收标准

- `/m/products` 展示真实商品。
- `/m/products/:id` 展示商品详情、图片、价格和库存。
- 未选择 SKU、库存为 0 或数量不合法时不能加入购物车。
- 加入购物车成功后有明确提示，并可以进入 `/m/cart`。
- 不擅自修改已经冻结的后端接口。

### 3. 组员 3：用户、地址与购物车模块

#### 建议文件

```text
frontend/src/views/mobile/MobileCartView.vue
frontend/src/views/mobile/MobileAddressView.vue
```

#### 今日工作

1. 完成购物车商品展示。
2. 完成单选、全选、数量修改和删除。
3. 完成选中商品金额合计。
4. 完成“去结算”校验和跳转。
5. 完成地址列表、新增、编辑、删除和默认地址。
6. 处理库存不足、购物车为空和未选择商品等异常状态。
7. 若以上 P0 任务在联调前完成，再将 `/m/profile` 替换为真实个人中心；不得因此推迟购物车和地址交付。

#### 验收标准

- `/m/cart` 不再是占位页。
- 购物车数量修改后金额正确更新。
- 数量不能小于 1，不能超过库存。
- 未选择商品时不能进入结算页。
- `/m/addresses` 支持地址增删改查和默认地址规则。
- 地址可以提供给确认订单页面选择。

> 今天优先保证手机购物主链路。后台用户管理和 PC 个人资料页面虽然仍未完成，但排入下一批任务，避免组员 3 当天任务过载。

### 4. 组员 4：订单、支付与 C03 模块

#### 建议文件

```text
frontend/src/views/mobile/MobileCheckoutView.vue
```

#### 今日工作

1. 从购物车读取已选择商品。
2. 展示商品、数量、单价和合计金额。
3. 展示并选择收货地址。
4. 支持填写订单备注。
5. 调用 `POST /orders` 创建订单。
6. 创建成功后跳转 `/m/orders/:id/pay`。
7. 对已经完成的订单列表、详情、取消、模拟支付和确认收货只做回归测试，不重复开发。
8. 整理并验证 C03 关键边界场景：超时支付、库存不足、重复操作和非法订单状态流转。

#### 验收标准

- `/m/checkout` 不再是占位页。
- 未选择商品或地址时不能提交订单。
- 提交期间按钮不可重复点击。
- 下单金额与后端返回金额一致。
- 下单成功后可以支付，并在订单列表看到最新状态。
- 已取消订单不能支付，已完成订单不能重复确认收货。

## 四、今日时间安排

| 时间 | 工作内容 | 负责人 |
|---|---|---|
| 09:00—09:30 | 同步最新基线、创建功能分支、确认只修改各自负责文件 | 全体组员 |
| 09:30—12:00 | 分别完成商品、购物车/地址、确认订单三个 P0 页面主体 | 组员 2、组员 3、组员 4 |
| 12:00 | 第一次检查：页面可以独立打开并展示真实接口数据 | 祝意远检查 |
| 14:00—16:00 | 完成交互、异常状态、移动端适配和模块自测 | 组员 2、组员 3、组员 4 |
| 16:00 | 提交功能分支并发送变更说明 | 组员 2、组员 3、组员 4 |
| 16:00—18:00 | Code Review、统一接路由、全链路测试和缺陷修复 | 祝意远组织，全体组员配合 |
| 18:00 前 | 提交个人日报 | 全体组员 |

## 五、代码协作规则

1. 禁止多人同时修改 `frontend/src/router/index.js`；业务负责人只提供页面组件和建议路由，由组长统一接入。
2. 不提交 `node_modules/`、`dist/`、IDE 配置、个人密码或本地环境文件。
3. 不使用硬编码管理员账号自动登录。
4. 不随意修改冻结接口；确需修改时，先说明原因、影响页面和兼容方案。
5. 每个提交只包含本模块相关文件，提交信息必须说明功能。
6. 提交前至少执行：

```bash
cd eshop-release-v1.0/frontend
npm run build
```

7. 功能分支交付时必须说明：
   - 修改了哪些文件；
   - 完成了哪些功能；
   - 使用了哪些接口；
   - 如何测试；
   - 仍有什么问题。

## 六、组长集成验证

接收三位组员代码后，由组长执行：

```powershell
cd eshop-release-v1.0
docker compose up -d --build
.\scripts\api-smoke-test.ps1
```

必须手工验证完整业务流程：

```text
注册/登录
→ 浏览和搜索商品
→ 选择 SKU
→ 加入购物车
→ 修改数量并选择商品
→ 选择收货地址
→ 确认并提交订单
→ 模拟支付
→ 查看订单
→ 后台发货
→ 手机确认收货
```

## 七、今日结束条件

以下条件全部满足，今日目标才算完成：

- 手机商品列表和详情已接入真实接口。
- 手机购物车和地址页面已接入真实接口。
- 手机确认订单能够创建真实订单。
- 手机订单支付与状态查询正常。
- 前端生产构建通过。
- 手机端完整交易主链路至少成功执行一次。
- 每位组员提交有效代码、变更说明和当日日报。

以下任务不作为今天 P0 验收条件，但必须保留在下一批计划中：

- 手机个人中心。
- Web 后台用户管理。
- PC 个人资料维护。
- 浏览器兼容性、弱网与移动尺寸回归。
- 项目总结、测试报告补充、演示脚本和答辩 PPT。
