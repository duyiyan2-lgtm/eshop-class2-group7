# 测试报告

> 组别：24 级全栈 2 班第 7 组　汇总人：祝意远　更新日期：2026-07-30　版本：v1.6
> 截止：第 4 周周三

## 一、测试概况

| 项目 | 内容 |
|------|------|
| 测试轮次 | 共 17 轮（含全站统一视觉最终回归） |
| 测试时间 | 2026-07-23 至 2026-07-30 |
| 测试人员 | 全组；最终集成回归：祝意远 |
| 测试环境 | Windows 11、JUnit/MockMvc/H2、Docker Desktop、MySQL 8.4、Nginx |

## 二、用例执行统计

| 模块 | 用例总数 | 通过 | 失败 | 阻塞 | 通过率 |
|------|----------|------|------|------|--------|
| 用户模块 | | | | | |
| 商品模块 | | | | | |
| 购物车 | | | | | |
| 订单模块 | | | | | |
| 支付模块 | | | | | |
| 后台管理 | | | | | |
| **合计** | | | | | |

## 三、缺陷统计

| 级别 | 提出 | 已修复 | 遗留 |
|------|------|--------|------|
| 致命 | | | |
| 严重 | | | |
| 一般 | | | |
| 轻微 | | | |

### 遗留缺陷说明

| 缺陷编号 | 描述 | 级别 | 遗留原因 | 规避方案 |
|----------|------|------|----------|----------|
| | | | | |

## 四、典型缺陷分析
（挑 2~3 个有代表性的缺陷，写清：现象 → 定位过程 → 根因 → 修复方式 → 反思）

## 五、测试结论

（系统是否达到验收标准，是否可以进入验收，风险提示）

## 六、基础框架回归记录（2026-07-23）

本节记录项目骨架、登录鉴权和部署配置的第一轮自动化回归。

| 检查项 | 结果 | 说明 |
|---|---|---|
| 后端编译与打包 | 通过 | Maven `test package` 成功生成可执行 JAR |
| 后端自动化测试 | 通过 | 13 个自动化测试（11 个业务测试 + 2 个接口契约测试），失败 0、错误 0 |
| 匿名访问 `/api/auth/me` | 通过 | 返回 401，不再返回 500 |
| 管理员登录与查询本人 | 通过 | 登录 200，携带 JWT 查询本人 200 |
| 管理员日志接口 | 通过 | 管理员 200，普通用户 403，匿名用户 401 |
| 健康检查 | 通过 | `/api/actuator/health` 返回 200 / UP |
| MySQL 8.4 + Flyway | 通过 | 在一次性 MySQL 8.4 实例完成迁移和真实启动 |
| 前端生产构建 | 通过 | PC、移动 H5、后台登录三个入口均返回 200 |
| 前端依赖审计 | 通过 | 生产依赖高危漏洞 0 |
| Docker Compose 配置 | 通过 | `docker compose config --quiet` |
| 前端 Docker 镜像 | 通过 | 成功构建 `eshop-class2-group7-frontend:latest` |
| 后端 Docker 镜像 | 通过 | 镜像构建成功，容器健康检查为 healthy |

本轮已修复两个严重缺陷：

1. MySQL 8.4 默认认证下 JDBC 连接失败：增加可配置的公钥获取参数。
2. `/auth/**` 被整体放行导致匿名访问 `/auth/me` 空指针：仅公开注册和登录接口，其余认证接口要求 JWT。

当前结论：基础框架与后端业务主流程均可运行，进入三端前端业务页面联调阶段。

## 七、后端业务闭环回归（2026-07-23）

| 检查项 | 结果 | 说明 |
|---|---|---|
| 分类与商品 | 通过 | 管理员创建分类、商品和 SKU，商品上架后匿名端可查询 |
| SKU 库存 | 通过 | 下单使用条件更新扣减库存，购买 2 件后库存从 10 变为 8 |
| 购物车与地址 | 通过 | 普通用户可加购、选择结算项和维护默认收货地址 |
| 创建订单 | 通过 | 订单金额按 SKU 单价与数量计算，并保存商品、规格、地址快照 |
| 模拟支付 | 通过 | 待支付订单生成唯一支付记录并进入已支付状态 |
| 发货与收货 | 通过 | 管理员发货后，用户确认收货，最终状态为 `COMPLETED` |
| 后台用户管理 | 通过 | 管理员可检索和禁用普通用户，被禁用用户无法重新登录 |
| 操作日志 | 通过 | 商品、用户、发货和文件等后台操作可查询 |
| 图片上传 | 通过 | PNG 上传成功，返回地址经 Nginx 访问 HTTP 200 |
| Flyway V2 迁移 | 通过 | 持久化 MySQL 从 V1 升级至 V2，9 张业务表创建成功 |
| Docker 实际交易链 | 通过 | 金额 59.80、库存 6→4、支付成功、订单最终完成 |

本轮没有遗留阻塞性后端缺陷。支付为课程项目要求的模拟支付；订单超时自动取消已在后续 C03 挑战模块中完成，真实第三方支付与退款仍属于项目范围外的扩展功能。

## 八、后端边界与安全回归（2026-07-23）

边界集成测试与主流程测试持续扩充，当前合计 11 个业务测试，全部通过；另有 2 个 API v1.0 冻结契约测试。

| 检查项 | 结果 | 修复或验证内容 |
|---|---|---|
| 商品上架约束 | 通过 | 创建/修改商品都不能绕过“至少一个启用 SKU”规则 |
| 状态保持 | 通过 | PUT 请求省略 `status` 时不再意外重置分类、商品或 SKU 状态 |
| SKU 一致性 | 通过 | 不能禁用已上架商品的最后一个启用 SKU |
| 分类一致性 | 通过 | 防止分类循环；含上架商品时不能停用分类 |
| 购物车累计数量 | 通过 | 多次加入同一 SKU 后总数仍限制为 99 |
| 地址默认状态 | 通过 | 编辑地址时显式传 `isDefault=false` 可以正确取消默认 |
| 取消订单 | 通过 | 取消待支付订单后库存恢复，取消后的订单不能再次支付 |
| 越权访问 | 通过 | 普通用户不能读取其他用户订单或修改其他用户地址 |
| 用户禁用 | 通过 | 用户被禁用后，之前签发的 JWT 也不能继续访问 |
| 个人信息维护 | 通过 | 用户可修改昵称和手机号，随后查询结果保持一致 |
| 商品与分类删除 | 通过 | 上架商品不能直接删除；下架后清理 SKU/购物车引用，非空分类不能删除 |
| 后台取消订单 | 通过 | 管理员可取消待支付订单，库存正确回补 |
| 异常请求 | 通过 | 坏 JSON、缺失上传字段、伪造图片和超大文件均返回 4xx，不再误报 500 |
| Docker/MySQL | 通过 | 修复后镜像重建成功，容器 healthy，真实 MySQL 边界回归通过 |
| BCrypt UTF-8 边界 | 通过 | 超过 72 个 UTF-8 字节的密码返回 40002，不再触发 BCrypt 异常和 HTTP 500 |
| 分类层级状态 | 通过 | 禁止在停用父分类下启用子分类，存在启用子孙分类时禁止停用父分类 |

最终结论：当前后端核心商城闭环和主要边界规则达到三端联调条件。

## 九、C03 订单超时自动取消回归（2026-07-23）

| 检查项 | 结果 | 说明 |
|---|---|---|
| 过期订单扫描 | 通过 | 只查询截止时间之前且状态为待支付的订单 |
| 条件取消 | 通过 | 状态条件更新成功后才执行库存恢复 |
| 防重复恢复 | 通过 | 同一订单连续取消两次，第二次返回 false，库存只恢复一次 |
| 支付冲突 | 通过 | 自动取消后支付返回 40912；支付先成功时取消条件不成立 |
| 多实例安全 | 通过 | 并发扫描依赖数据库条件更新，不依赖单机锁 |
| 状态日志 | 通过 | 记录 `PENDING_PAYMENT → CANCELED` 和超时原因 |
| 商品删除保护 | 通过 | 商品存在待支付、已支付或已发货订单时禁止删除 |

C03 的实现原理、配置和现场演示步骤见 `docs/02-设计文档/C03订单超时自动取消设计.md`。

## 十、完整回归与缺陷闭环（2026-07-23）

本轮同时使用 H2 集成测试和 Docker 中的真实 MySQL 8.4 进行验证。真实环境冒烟脚本为 `scripts/api-smoke-test.ps1`，默认在完成后精确清理本次生成的测试用户、订单、地址、商品和分类。

| 检查项 | 结果 | 说明 |
|---|---|---|
| 后端自动化测试 | 通过 | 13 项，失败 0、错误 0 |
| API 冻结契约 | 通过 | 48 个 HTTP 方法/路径及 13 个请求 DTO 字段集合未发生漂移 |
| 真实 MySQL API 冒烟 | 通过 | 41 个检查点，覆盖完整交易链、权限、越权、重复操作、支付截止时间和库存一致性 |
| 前端生产构建 | 通过 | Vite 构建成功 |
| 前端依赖审计 | 通过 | 使用 npm 官方审计端点，生产依赖漏洞 0 |
| 三端直接路由 | 通过 | `/pc`、`/pc/login`、`/m`、`/m/login`、`/admin`、`/admin/login`、`/admin/logs` 均返回 200 |
| Docker Compose | 通过 | 配置校验成功，前端、后端和 MySQL 容器正常运行 |
| 演示数据 | 通过 | 测试数据清理后保持 5 个分类、32 个商品、32 个 SKU |

本轮修复的缺陷：

1. **多字节密码触发 HTTP 500**：原校验使用字符数，而 BCrypt 限制 72 个 UTF-8 字节。现已在注册与登录前校验字节长度，超限返回 HTTP 400 / 业务码 40002。
2. **停用父分类下可启用子分类**：会在消费者端形成没有可见父节点的孤儿分类。现已要求启用分类的全部上级均启用；父分类存在启用子孙分类时禁止停用。
3. **真实 API 脚本兼容性与数据污染**：修复 Windows PowerShell 5.1 对无 BOM 中文脚本的解析问题，并增加测试数据自动清理。
4. **支付扫描间隙仍可晚付**：支付更新增加订单创建时间条件，超过支付时限后即使扫描器尚未执行也返回 40926。
5. **SKU 缺少安全删除闭环**：新增管理员删除 SKU 接口；商品必须先下架，存在未完成订单时返回 40927，防止库存回补目标被提前删除。

结论：现有后端主流程、关键边界和 Docker 真实运行环境未发现遗留阻塞性缺陷，可以继续进行三端业务页面开发与联调。

## 十一、手机端公共框架与完整回归（2026-07-24）

本轮在手机 H5 公共布局和路由完成后执行。由于已有开发容器占用 `8088`，使用同一份 Compose 配置在 `18088` 启动独立回归环境，未停止或修改原有容器。

| 检查项 | 结果 | 说明 |
|---|---|---|
| 后端自动化测试 | 通过 | 使用 Maven 容器执行 13 项测试，失败 0、错误 0 |
| 前端生产构建 | 通过 | Vite 转换 2077 个模块并生成生产产物 |
| 前端生产依赖审计 | 通过 | 临时使用 npm 官方审计端点，发现漏洞 0 |
| Docker 镜像构建 | 通过 | 前端和后端镜像均成功构建 |
| 容器健康 | 通过 | MySQL、后端和前端服务均正常运行，后端健康检查为 healthy |
| 真实 MySQL API 冒烟 | 通过 | 41 个检查点全部通过，测试数据自动清理 |
| 手机端直接路由 | 通过 | 商品、详情、购物车、地址、结算、订单、支付、个人中心入口均返回 200 |
| 浏览器实际渲染 | 通过 | 手机首页、商品详情、登录页、顶部栏和四项底部导航正确渲染 |
| 登录保护 | 通过 | 未登录访问购物车、订单和个人中心均进入手机登录页 |
| 登录返回地址 | 通过 | 从商品详情进入登录页时保留原页面地址 |
| 源码检查 | 通过 | 无 Git 冲突标记，构建产物、本地环境和运行数据均被忽略 |

本轮修复的公共框架缺陷：

1. **登录后丢失原页面**：手机端顶部登录入口原先未携带当前地址，现通过 `redirect` 参数保留来源页面。
2. **退出接口失败时页面不跳转**：远程退出暂时失败会抛出异常并中断导航；现始终清除本地登录态并允许页面返回登录入口。
3. **详情页返回方向错误**：非主导航页面原先固定返回商品首页；现优先使用有效浏览历史，无历史时才回到手机商品首页。

结论：手机端公共导航、路由、权限保护和占位页面可以交付各模块负责人继续接入业务页面；现有后端、PC 端和 Web 管理端未发现新的阻塞性缺陷。

## 十二、扩展功能最终回归（2026-07-28）

在完整商城闭环基础上，本轮完成商品收藏与评价前端、评分统计、库存预警、订单导出、后台评价审核和热销商品排行。

| 检查项 | 结果 | 说明 |
|---|---|---|
| 后端自动化 | 通过 | 9 个测试类、19 项测试，失败 0、错误 0、跳过 0 |
| 前端生产构建 | 通过 | Vite 成功转换 2172 个模块 |
| 真实 MySQL API | 通过 | 86 个检查点全部通过并自动清理测试数据 |
| 评价审核 | 通过 | 权限、筛选、隐藏/恢复、公开列表及评分联动正常 |
| 热销排行 | 通过 | 有效订单状态、时间范围、下架商品边界与排序正常 |
| 页面直达 | 通过 | PC、手机、后台及评价管理入口均返回 HTTP 200 |
| 容器健康 | 通过 | MySQL 和后端健康，前端正常运行 |
| 运行日志 | 通过 | 后端最近日志无 ERROR 或 Exception |

结论：当前项目已经达到可演示、可测试、可部署状态，新增扩展未破坏商城交易链。

## 十三、平台账号与商家工作台最终回归（2026-07-30）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 后端自动化 | 通过 | 11 个测试类、22 项测试，失败 0、错误 0、跳过 0 |
| 平台账号管理 | 通过 | 买家/卖家统计、筛选、新增、启停与角色切换正常 |
| 权限隔离 | 通过 | 卖家不能管理账号，买家不能进入商家工作台，管理员账号受保护 |
| 权限即时生效 | 通过 | 修改角色或禁用后，旧 JWT 后续请求立即按新状态处理 |
| 前端生产构建 | 通过 | Vite 成功转换 2187 个模块 |
| 数据库迁移 | 通过 | Flyway V1～V8 在 MySQL 8.4 全部成功 |
| 真实 MySQL API | 通过 | 122 个检查点全部通过，测试数据自动清理 |
| 页面直达 | 通过 | `/admin/users`、`/seller/login`、`/seller/products`、`/seller/orders` 均为 HTTP 200 |
| 容器与日志 | 通过 | MySQL、后端健康，最近日志无 ERROR、Exception 或 HTTP 500 |

结论：平台管理员、商家和买家三类权限边界清晰，原后台已安全复用为商家工作台，可以进入验收演示。

## 十四、买家与商家区分注册最终回归（2026-07-30）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 买家注册 | 通过 | `role=USER` 立即启用，省略角色仍兼容为买家 |
| 商家注册 | 通过 | `role=SELLER` 创建待审核账号，审核前不能登录 |
| 平台审核 | 通过 | 管理员启用后商家可登录工作台 |
| 权限安全 | 通过 | 公开注册传 `ADMIN` 被拒绝 |
| 前端与分流 | 通过 | PC/手机可选身份，商家入口可申请；登录后按角色跳转 |
| 自动化 | 通过 | 后端 22/22、前端 2187 modules、真实 MySQL API 126/126 |

结论：买家、商家注册流程已明确区分，并通过平台审核避免未授权账号直接操作商城数据。

## 十五、商家发货最终回归（2026-07-30）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 商家订单页面 | 通过 | `/seller/orders` 可访问，待发货订单显示“确认发货” |
| 发货权限 | 通过 | `SELLER` 可以发货，普通买家调用发货接口返回 `403` |
| 订单状态 | 通过 | 已支付订单由 `PAID` 更新为 `SHIPPED`，同步记录发货时间 |
| 操作审计 | 通过 | 商家发货记录操作人，并写入“商家发货”状态日志 |
| 买家收货 | 通过 | 发货后买家可确认收货，订单最终进入 `COMPLETED` |
| 后端自动化 | 通过 | 11 个测试类、22 项测试，失败 0、错误 0、跳过 0 |
| 前端生产构建 | 通过 | Vite 成功转换 2187 个模块 |
| 真实 MySQL API | 通过 | 部署后 126 个检查点全部通过，其中商家账号完成实际发货 |
| 页面与日志 | 通过 | 健康检查 `UP`、商家订单页 HTTP 200，最近日志无 ERROR、Exception 或 HTTP 500 |

结论：商家发货已形成“买家支付 → 商家确认发货 → 买家确认收货”的完整角色化闭环。

## 十六、三端统一入口最终回归（2026-07-30）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 统一入口 | 通过 | `/` 直接返回 HTTP 200，不再被 Nginx 重定向到 `/pc` |
| 集中切换 | 通过 | 同一页面集中提供 PC、手机 H5、商家工作台和平台管理入口 |
| 角色识别 | 通过 | 页面展示当前账号与角色，可用工作台直接进入，角色不符时提示切换账号 |
| 切换提示 | 通过 | 自定义居中弹窗展示当前身份与目标入口，支持关闭按钮、遮罩、ESC 和移动端适配 |
| 全局操作反馈 | 通过 | 消息顶部悬浮、确认框居中展示，成功、警告、错误等状态保持统一视觉 |
| 手机购物车反馈 | 通过 | 加入和移除商品均显示顶部成功提示；删除前使用带遮罩的居中二次确认框 |
| 权限隔离 | 通过 | 统一入口不改变路由守卫和后端鉴权，买家不能直接进入商家或平台管理 |
| 各端返回入口 | 通过 | PC、手机、商家、平台布局和登录页均可返回统一入口 |
| 页面直达 | 通过 | `/`、`/pc/products`、`/m/products`、`/seller/login`、`/seller`、`/admin/login`、`/admin/users` 均为 HTTP 200 |
| Nginx 配置 | 通过 | `nginx -t` 成功，SPA history 路由回退正常 |
| 前端生产构建 | 通过 | Vite 成功转换 2196 个模块 |
| 真实 MySQL API | 通过 | 126 个检查点全部通过，已有真实商家账号不会干扰测试数据定位 |
| 容器与日志 | 通过 | 后端健康检查 `UP`，最近日志无 ERROR、Exception 或 HTTP 500 |

结论：三端入口已统一整合，用户能够在一个页面按身份快速切换，同时保持原有权限边界。

## 十七、全站统一视觉最终回归（2026-07-30）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 设计基准 | 通过 | 以统一入口的深蓝渐变、玻璃卡片、浅蓝高亮和细边框为全站视觉基准 |
| PC 消费者端 | 通过 | 导航、筛选、商品、购物车、地址、订单、支付、评价和个人资料使用统一主题 |
| 手机 H5 端 | 通过 | 顶部栏、底部导航、商品、购物车、订单、表单、弹窗和反馈组件完成深色适配 |
| 商家工作台 | 通过 | 侧边导航、经营看板、分类、商品、库存、订单、评价、日志及抽屉表单完成深色适配 |
| 平台管理端 | 通过 | 平台导航、买家/卖家统计、筛选、表格和账号操作组件完成深色适配 |
| 登录与注册 | 通过 | 买家、手机、商家和管理员入口统一为带渐变顶边的玻璃登录卡片 |
| 业务状态颜色 | 通过 | 保留成功、库存预警、订单状态、价格和危险操作的语义色，深色背景下可辨识 |
| 响应式布局 | 通过 | 桌面与手机尺寸完成截图抽查，导航、卡片、表单和按钮未出现遮挡 |
| 页面直达 | 通过 | 统一入口及 PC、手机、商家、平台共 38 个路由均返回 HTTP 200 |
| 前端生产构建 | 通过 | Vite 成功转换 2196 个模块 |
| 真实 MySQL API | 通过 | 126 个检查点全部通过，主题改造未影响任何业务流程 |
| 服务与日志 | 通过 | Nginx 配置正常、后端健康检查 `UP`，最近日志无 ERROR、Exception 或 HTTP 500 |

结论：所有业务端已统一到入口页的设计语言，并完成桌面、手机、路由、构建和完整业务回归。
