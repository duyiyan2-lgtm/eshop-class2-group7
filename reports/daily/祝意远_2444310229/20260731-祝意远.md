# 日报 - 祝意远 - 2026-07-31

> 学号：2444310229
>
> 角色：组长 / 架构员 / 部署负责人
>
> 分支：`chore/extension-integration`

## 今日完成

1. 检查阿里云服务器资源和运行环境，确认服务器规格为 2 vCPU、2 GiB 内存、40 GiB 云盘，并验证 Git、Docker 26.1.3、Docker Compose 2.27.0 可用。
2. 在服务器建立项目目录 `/www/wwwroot/eshop`，从个人 Gitee 仓库拉取 `chore/extension-integration` 分支，并确认后端、前端、Docker Compose、文档和脚本目录完整。
3. 使用 Docker Compose 构建并启动 MySQL、Spring Boot 后端和 Nginx 前端，排查后端首次退出问题；最终 MySQL 与后端均为 `healthy`，前端容器正常运行并映射服务器 8088 端口。
4. 验证 `/api/actuator/health` 返回 `{"status":"UP"}`，验证服务器本机访问前端返回 HTTP 200，确认容器网络、数据库连接、Flyway 迁移和 Nginx 反向代理链路正常。
5. 检查阿里云安全组和宝塔面板端口配置，确认 80、443、8088 等部署所需端口状态，为域名反向代理和 HTTPS 接入做准备。
6. 处理生产管理员初始化与密码配置，关闭后续自动创建管理员的开关，采用 BCrypt 保存密码，避免在配置文件和命令输出中保留管理员明文密码。
7. 整理服务器部署路径、启动命令、健康检查命令和日志排查方法，为后续更新代码和故障恢复保留可重复操作步骤。

## 遇到的问题

| 问题描述 | 解决状态 | 解决方式/后续动作 |
|----------|----------|-------------------|
| 在项目目录外执行 `docker compose` 提示找不到配置文件 | 已解决 | 统一进入 `/www/wwwroot/eshop/eshop-class2-group7/eshop-release-v1.0` 后再执行 Compose 命令 |
| 后端容器首次启动后退出，导致前端依赖无法启动 | 已解决 | 查看 `docker compose logs backend`，修正生产环境变量和管理员初始化配置后重新构建 |
| 服务器 2 GiB 内存构建镜像时余量较小 | 已缓解 | 检查现有 Swap，保留分阶段构建和日志监控，避免同时运行无关服务 |
| 前端容器尚未就绪时公网访问出现 502 | 已解决 | 等待后端健康后重新启动前端，使用服务器本机 `curl` 和 `docker compose ps` 双重确认 |
| 服务器没有安装 `nano` | 已解决 | 改用已有命令行工具和可重复的配置修改命令，不额外安装非必要软件 |

## 明日计划

1. 在宝塔面板为 `eshop.zhuyiyuan9.top` 建立反向代理并配置 HTTPS 证书。
2. 从公网验证统一入口、PC、手机、商家和平台管理页面。
3. 完成管理员登录、买家注册下单、商家发货和买家收货评价的线上冒烟测试。
4. 完成生产环境安全检查，避免数据库密码、JWT 密钥和管理员密码进入 Git。

## 今日工时

约 8 小时。
