# 日报 - 祝意远 - 2026-07-22

## 今日完成

1. 作为组长完成项目范围梳理，明确系统需要交付 PC 消费者端、Web 管理端和手机 H5 消费者端；确认手机端采用 Vue 3 + Vant 的 H5 页面，不开发原生 Android/iOS App。
2. 确定整体技术方案：前端使用 Vue 3、Vite、Element Plus 和 Vant，后端使用 Spring Boot、MyBatis-Plus、MySQL、JWT，使用 Docker Compose 统一运行环境。
3. 完成四人小组的模块分工和三周开发计划，明确组长负责项目脚手架、登录鉴权、公共组件、接口规范、集成联调及关键模块兜底。
4. 提前设计分类、商品、SKU、库存、购物车、收货地址、订单、模拟支付和后台操作日志等核心业务，确定商品采用“先创建草稿 → 添加 SKU → 再上架”的流程，降低后期增加规格和库存功能时的返工风险。
5. 建立前后端及 Docker 运行框架，完成 Node 22 Alpine 基础镜像拉取，并验证 Docker Desktop 能够正常连接镜像仓库。
6. 分支/提交说明：当日内容在本地工作区完成，尚未形成有效 Git commit；待项目仓库分支规范确认后迁移到 `feature/project-scaffold` 并拆分提交。

## 遇到的问题

| 问题描述 | 解决状态 | 解决方式/求助对象 |
|----------|----------|-------------------|
| 移动端要求不明确，不确定是否需要 APK、iOS 或小程序 | 已解决 | 根据教师已给要求确认只需完成手机端，不要求原生 App，因此采用 Vue 3 + Vant H5 |
| 担心后期增加 SKU、库存和订单状态时需要重构商品表和接口 | 已解决 | 在首版数据库和接口中提前纳入 SKU、库存扣减、订单状态流转和支付记录设计 |
| Docker 拉取 `node:22-alpine` 时访问 `auth.docker.io` 超时 | 已解决 | 检查 Docker Desktop 代理配置，调整为可用的系统代理后重新拉取镜像 |

## 明日计划

1. 完成 Spring Boot 后端基础工程、数据库迁移、统一返回结构、异常处理和 JWT 登录鉴权。
2. 按用户、商品、购物车、地址、订单和支付模块实现第一版接口，并整理接口清单。
3. 启动 MySQL、后端和前端容器，验证 Docker Compose 健康检查及三端路由入口。

## 今日工时

约 8 小时
